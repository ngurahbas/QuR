spring:
  application:
    name: QuR
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/qur_db
    username: qur_user
    password: qur_password
  datasource:
    url: jdbc:postgresql://localhost:5432/qur_db
    username: qur_user
    password: qur_password
    driver-class-name: org.postgresql.Driver